server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Serve static assets directly and route all other paths to index.html for React Router.
  location / {
    try_files $uri $uri/ /index.html;
  }
}