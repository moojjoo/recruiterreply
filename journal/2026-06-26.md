# 2026-06-26

## Goals
- Deploy RecruiterReply frontend
- Fix EC2 HTTPS issue
- Configure Route53

## Completed
- Added CloudFront distribution
- Updated GitHub Actions deployment
- Fixed Docker networking

## Problems
- EC2 port 443 refused connection
- SSL certificate not attached

## Tomorrow
- Configure ACM certificate
- Test production deployment
- Add health check endpoint

## Commands Used

aws s3 sync dist s3://recruiterreply.com
docker compose up -d

## Lessons Learned
CloudFront requires the origin to be reachable before the distribution becomes healthy.

## Import URLs
curl -sS https://recruiterreply.com -o /dev/null -w "%{http_code}\n" (expect 200)
curl -sS https://api.recruiterreply.com/health (expect status healthy)
curl -sS https://api.recruiterreply.com/health/db (confirms DB connectivity)
curl -sS https://api.recruiterreply.com/health/apis (confirms OpenAI config flag)
curl -sS https://api.recruiterreply.com/health/crud (full CRUD probe)

## Docker work
docker compose -f docker-compose.multi-env.yml ps
docker logs recruiterreply-backend-prod --tail 100
docker logs recruiterreply-postgres --tail 100
sudo nginx -t && sudo systemctl status nginx --no-pager
sudo certbot certificates (confirm TLS not near expiry)