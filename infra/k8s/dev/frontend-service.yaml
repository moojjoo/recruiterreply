apiVersion: v1
kind: Service
metadata:
  name: recruiterreply-frontend
  namespace: recruiterreply-dev
spec:
  selector:
    app: recruiterreply-frontend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
