apiVersion: v1
kind: Service
metadata:
  name: recruiterreply-backend
  namespace: recruiterreply-dev
spec:
  selector:
    app: recruiterreply-backend
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
