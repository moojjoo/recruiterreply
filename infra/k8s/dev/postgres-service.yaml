apiVersion: v1
kind: Service
metadata:
  name: recruiterreply-postgres
  namespace: recruiterreply-dev
spec:
  selector:
    app: recruiterreply-postgres
  ports:
    - name: postgres
      protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
